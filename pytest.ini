[pytest]
# Pytest Configuration for AEGIS RAG Sprint 2 Tests

# Test discovery patterns
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Test paths
testpaths = tests

# Markers for organizing tests
markers =
    unit: Unit tests (fast, mocked dependencies)
    integration: Integration tests (require external services)
    cloud: Cloud integration tests requiring API keys (DashScope, OpenAI - skip with -m "not cloud")
    slow: Slow tests (long-running)
    asyncio: Async tests
    e2e: End-to-end tests (Sprint 12 skeleton tests)
    gpu: GPU-accelerated tests (require CUDA/GPU hardware)
    docker: Tests requiring Docker containers (Qdrant, Neo4j, Ollama, Redis)
    stress: Stress and performance tests (high load scenarios)
    performance: Performance and benchmark tests (Sprint 48)
    sprint8: Sprint 8 critical path E2E tests (NO MOCKS per ADR-014)
    sprint9: Sprint 9 Memory + MCP E2E tests (NO MOCKS per ADR-014)
    sprint13: Sprint 13 Performance & Infrastructure tests (NO MOCKS per ADR-014)
    ingestion: Tests requiring ingestion dependencies (PIL, llama_index - skipped in unit CI)
    requires_llm: Tests requiring real LLM (Ollama) - skipped in CI (run locally with -m requires_llm)
    admin: Admin interface tests (Sprint 50 E2E tests)
    ui: UI-related tests
    sprint64: Sprint 64 VLM integration and domain training tests

# Async test configuration
asyncio_mode = auto

# Output options
addopts =
    -v
    --strict-markers
    --tb=short
    --show-capture=no
    -ra
    --maxfail=5
    --ignore=tests/e2e/archive

# Minimum code coverage threshold (80%)
# Uncomment to enforce coverage:
# --cov-fail-under=80

# Disable warnings for cleaner output (optional)
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning

# Log configuration
log_cli = false
log_cli_level = INFO
log_cli_format = %(asctime)s [%(levelname)8s] %(message)s
log_cli_date_format = %Y-%m-%d %H:%M:%S

# Timeout for tests (prevent hanging)
# Sprint 13 Feature 13.4: pytest-timeout installed (TD-29)
timeout = 300
timeout_method = thread

# Parallel execution (install pytest-xdist)
# Run with: pytest -n auto
# addopts = -n auto
